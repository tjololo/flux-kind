apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: glookroflux-system
spec:
  interval: 60m
  chart:
    spec:
      chart: kro
      version: 0.4.1
      sourceRef:
        kind: HelmRepository
        name: kro
        namespace: kro
  releaseName: kro
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace