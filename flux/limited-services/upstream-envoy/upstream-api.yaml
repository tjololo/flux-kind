apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: api-upstream
  namespace: limited-services
spec:
  endpoints:
    - fqdn:
        hostname: api.chucknorris.io
        port: 443
---
apiVersion: gateway.networking.k8s.io/v1
kind: BackendTLSPolicy
metadata:
  name: api-upstream-tls
  namespace: limited-services
spec:
  targetRefs:
    - group: gateway.envoyproxy.io
      kind: Backend
      name: api-upstream
  validation:
    wellKnownCACertificates: System
    hostname: api.chucknorris.io
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: upstream-api
  namespace: limited-services
spec:
  hostnames:
    - "kind.418.test"
  parentRefs:
    - name: eg
      namespace: envoy-gateway-system
      sectionName: web
  rules:
    - matches:
      - path:
          type: PathPrefix
          value: /chuck/api
      filters:
        - type: URLRewrite
          urlRewrite:
            hostname: api.chucknorris.io
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /jokes
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: api-upstream
