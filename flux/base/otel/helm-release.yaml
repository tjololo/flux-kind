apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: otel-operator
  namespace: flux-system
spec:
  interval: 60m
  chartRef:
    kind: OCIRepository
    name: otel-operator
    namespace: flux-system
  releaseName: otel-operator
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace